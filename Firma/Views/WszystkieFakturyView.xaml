<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Firma.Views"
             xmlns:Models="clr-namespace:Firma.Models" x:Class="Firma.Views.WszystkieFakturyView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DataGrid AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Numer" Width="120"/>
                <DataGridTextColumn Header="VAT" Width="120"/>
                <DataGridCheckBoxColumn Header="Opłacona" Width="120"/>
            </DataGrid.Columns>
            <Models:Faktura/>
        </DataGrid>

    </Grid>
</UserControl>
