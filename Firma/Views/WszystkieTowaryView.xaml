<local:WszystkieViewBase  
    x:Class="Firma.Views.WszystkieTowaryView" 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:local="clr-namespace:Firma.Views"
    xmlns:views="clr-namespace:Firma.Views"
    Height="600" Width="1100">
    
    <Grid>
        <DataGrid  AutoGenerateColumns="False"  ItemsSource="{Binding  TowaryList}">
            <DataGrid.Columns>
                <DataGridTextColumn  x:Name="id"  Binding="{Binding  Path=id}" Header="Nr"/> 
                <DataGridTextColumn  x:Name="kod"  Binding="{Binding  Path=kod}" Header="Kod"/> 
                <DataGridTextColumn  x:Name="nazwa"  Binding="{Binding  Path=nazwa}" Header="Nazwa"/>                
                <DataGridTextColumn  x:Name="stawkaVatSprzedazy"  Binding="{Binding Path=stawkaVatSprzedazy}"  Header="Stawka  Vat  Sprzedazy"/>
                <DataGridTextColumn  x:Name="stawkaVatZakupu"  Binding="{Binding Path=stawkaVatZakupu}"  Header="Stawka  Vat  Zakupu"/>
                <DataGridTextColumn  x:Name="cena"  Binding="{Binding  Path=cena}" Header="Telefon"/>
                <DataGridTextColumn  x:Name="marza"  Binding="{Binding  Path=marza}" Header="Marza"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>